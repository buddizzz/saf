# Cloudflare Pages — تطبيق أصحاب المحلات (safapp.net)
name: saf-web
compatibility_date = "2024-11-06"
pages_build_output_dir = "dist"

[vars]
# يضبطه الـ CI / لوحة Cloudflare عند الربط
# VITE_API_BASE يُحقن وقت البناء إن لزم
